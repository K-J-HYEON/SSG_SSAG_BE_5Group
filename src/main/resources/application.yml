spring:
    jpa:
        generate-ddl: true
        properties:
            hibernate:
                dialect: org.hibernate.dialect.MySQL5InnoDBDialect
                show_sql: true
        show_sql: 'false'
        database: mysql
        hibernate:
            ddl-auto: update

    profiles:
        include: aws, db, secret